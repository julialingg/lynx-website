diff --git a/src/components/go/example-preview/components/code.tsx b/src/components/go/example-preview/components/code.tsx
index f7414c02..a9ad6704 100644
--- a/src/components/go/example-preview/components/code.tsx
+++ b/src/components/go/example-preview/components/code.tsx
@@ -85,6 +85,12 @@ export const Code: FC<CodeProps> = ({
         lang={language}
         onRendered={() => {
           scrollToFirstHighlightLine();
+          // Ensure code block is accessible
+          const pres = containerRef.current?.querySelectorAll('pre.shiki');
+          pres?.forEach(el => {
+            if (!el.hasAttribute('role')) el.setAttribute('role', 'region');
+            if (!el.hasAttribute('aria-label')) el.setAttribute('aria-label', 'Code snippet');
+          });
         }}
         code={val}
         shikiOptions={{
@@ -93,10 +99,10 @@ export const Code: FC<CodeProps> = ({
             transformerLineNumber(),
             ...(highlightVal
               ? [
-                  transformerRuntimeMetaHighlight({
-                    highlightVal,
-                  }),
-                ]
+                transformerRuntimeMetaHighlight({
+                  highlightVal,
+                }),
+              ]
               : []),
           ],
         }}
diff --git a/src/components/go/example-preview/components/shiki-transformer.ts b/src/components/go/example-preview/components/shiki-transformer.ts
index 2c411d8e..dd4f0620 100644
--- a/src/components/go/example-preview/components/shiki-transformer.ts
+++ b/src/components/go/example-preview/components/shiki-transformer.ts
@@ -51,7 +51,8 @@ export function transformerRuntimeMetaHighlight(
       return node;
     },
     pre(hast) {
-      this.addClassToHast(hast, 'has-highlighted');
+
+      if (!hast.properties) hast.properties = {}; hast.properties.tabIndex = 0; hast.properties.role = 'region'; hast.properties['aria-label'] = 'Code snippet';
       return hast;
     },
   };
diff --git a/src/components/ui/select.tsx b/src/components/ui/select.tsx
index a5afddf5..14d417ab 100644
--- a/src/components/ui/select.tsx
+++ b/src/components/ui/select.tsx
@@ -20,6 +20,7 @@ const SelectTrigger = React.forwardRef<
       "sh-flex sh-h-10 sh-w-full sh-items-center sh-justify-between sh-rounded-md sh-border sh-border-input sh-bg-background sh-px-3 sh-py-2 sh-text-sm sh-ring-offset-background placeholder:sh-text-muted-foreground focus:sh-outline-none focus:sh-ring-2 focus:sh-ring-ring focus:sh-ring-offset-2 disabled:sh-cursor-not-allowed disabled:sh-opacity-50 [&>span]:sh-line-clamp-1",
       className
     )}
+    aria-controls={undefined}
     {...props}
   >
     {children}
@@ -75,7 +76,7 @@ const SelectContent = React.forwardRef<
       className={cn(
         "sh-relative sh-z-50 sh-max-h-96 sh-min-w-[8rem] sh-overflow-hidden sh-rounded-md sh-border sh-bg-popover sh-text-popover-foreground sh-shadow-md data-[state=open]:sh-animate-in data-[state=closed]:sh-animate-out data-[state=closed]:sh-fade-out-0 data-[state=open]:sh-fade-in-0 data-[state=closed]:sh-zoom-out-95 data-[state=open]:sh-zoom-in-95 data-[side=bottom]:sh-slide-in-from-top-2 data-[side=left]:sh-slide-in-from-right-2 data-[side=right]:sh-slide-in-from-left-2 data-[side=top]:sh-slide-in-from-bottom-2",
         position === "popper" &&
-          "data-[side=bottom]:sh-translate-y-1 data-[side=left]:sh--translate-x-1 data-[side=right]:sh-translate-x-1 data-[side=top]:sh--translate-y-1",
+        "data-[side=bottom]:sh-translate-y-1 data-[side=left]:sh--translate-x-1 data-[side=right]:sh-translate-x-1 data-[side=top]:sh--translate-y-1",
         className
       )}
       position={position}
@@ -86,7 +87,7 @@ const SelectContent = React.forwardRef<
         className={cn(
           "sh-p-1",
           position === "popper" &&
-            "sh-h-[var(--radix-select-trigger-height)] sh-w-full sh-min-w-[var(--radix-select-trigger-width)]"
+          "sh-h-[var(--radix-select-trigger-height)] sh-w-full sh-min-w-[var(--radix-select-trigger-width)]"
         )}
       >
         {children}
diff --git a/theme/index.tsx b/theme/index.tsx
index ce13db5c..e6c8dc45 100644
--- a/theme/index.tsx
+++ b/theme/index.tsx
@@ -1,4 +1,4 @@
-import { useCallback, useEffect, useMemo, useState } from 'react';
+import { useCallback, useEffect, useLayoutEffect, useMemo, useState } from 'react';
 import { useLocation, usePageData } from 'rspress/runtime';
 import {
   HomeLayout as BaseHomeLayout,
@@ -28,6 +28,33 @@ function Layout() {
       subsite ? subsite.value : 'guide',
     );
   }, [pathname]);
+  // ARIA fix for code snippets
+  useLayoutEffect(() => {
+    document.querySelectorAll('pre[tabindex="0"]').forEach((el) => {
+      el.removeAttribute('tabindex');
+      el.setAttribute('role', 'region');
+      el.setAttribute('aria-label', 'Code snippet');
+    });
+  }, []);
+  // ARIA fix for social links
+  useEffect(() => {
+    document.querySelectorAll('a.social-links').forEach((el) => {
+      if (!el.hasAttribute('aria-label')) {
+        const href = el.getAttribute('href') || '';
+        let label = 'Social link';
+        if (href.includes('x.com') || href.includes('twitter.com')) {
+          label = 'LynxJS on X';
+        } else if (href.includes('github.com')) {
+          label = 'LynxJS on GitHub';
+        } else if (href.includes('npmjs.com')) {
+          label = 'LynxJS on npm';
+        } else if (href.includes('discord.gg') || href.includes('discord.com')) {
+          label = 'LynxJS on Discord';
+        }
+        el.setAttribute('aria-label', label);
+      }
+    });
+  }, []);
 
   return (
     <BaseLayout
